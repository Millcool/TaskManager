import Foundation

enum StudyContent_ML {
    static func makeGroup() -> StudyGroup {
        StudyGroup(
            title: "Машинное обучение и ИИ",
            subtitle: "Линал, статистика, классическое ML, нейросети, NLP и CV",
            iconSystemName: "brain.head.profile",
            topics: [linearAlgebra, probability, classicalML, neuralNetworks, optimization, deepLearning]
        )
    }

    static var linearAlgebra: StudyTopic {
        StudyTopic(
            title: "Линейная алгебра для ML",
            summary: "Векторы, матрицы, нормы, SVD, PCA, собственные значения.",
            questions: [
                StudyQuestion(
                    question: "Что такое собственные значения и собственные векторы, зачем они в ML?",
                    answer: "Для квадратной матрицы A собственный вектор v ≠ 0 и собственное значение λ такие, что Av = λv. Геометрически — направления, вдоль которых линейное преобразование действует как масштабирование. В ML используются для PCA (собственные векторы ковариационной матрицы — главные компоненты), спектральной кластеризации, PageRank, уменьшения размерности.",
                    examples: "Матрица ковариаций X^T X имеет ортогональные собственные векторы (она симметричная) — они и есть оси максимальной дисперсии."
                ),
                StudyQuestion(
                    question: "Что такое SVD и как оно применяется?",
                    answer: "Сингулярное разложение: любая матрица A (m×n) представляется как UΣV^T, где U (m×m) и V (n×n) ортогональные, Σ (m×n) диагональная с сингулярными значениями σ₁ ≥ σ₂ ≥ ... ≥ 0. Связь с собственными значениями: σᵢ² — собственные значения AA^T и A^TA. Применение: усечённое SVD — низкоранговое приближение (лучшее в норме Фробениуса по теореме Экарта–Янга).",
                    examples: "PCA = SVD центрированной матрицы данных. Рекомендательные системы (Netflix Prize) — SVD на матрице (юзер × фильм). LSI в NLP — SVD на term-document матрице."
                ),
                StudyQuestion(
                    question: "Какие бывают нормы векторов и матриц?",
                    answer: "Векторные: L¹ (сумма модулей, спарсность), L² (евклидова, наиболее частая), L^∞ (максимум). Матричные: Фробениуса (‖A‖_F² = Σ aᵢⱼ²), спектральная (‖A‖₂ = σ_max), ядерная (‖A‖_* = Σσᵢ). L¹ регуляризация (Lasso) даёт разреженные веса; L² (Ridge) — гладкие, но ненулевые.",
                    examples: "Lasso в линейной регрессии = min ‖y - Xw‖₂² + λ‖w‖₁ — отбор признаков. Ridge: λ‖w‖₂² — предотвращает взрывающиеся веса при мультиколлинеарности."
                ),
                StudyQuestion(
                    question: "PCA: как работает и что делает?",
                    answer: "PCA ищет ортогональные направления максимальной дисперсии данных. Алгоритм: 1) центрируем X. 2) Считаем ковариационную матрицу C = X^TX/(n-1). 3) Собственные векторы C с наибольшими собственными значениями = главные компоненты. 4) Проекция X на k компонент = снижение размерности. Дисперсия, объясняемая k-й компонентой, пропорциональна λₖ.",
                    examples: "MNIST (784 признака) → PCA до 50 компонент сохраняет ~90% дисперсии, ускоряет последующие модели. Но PCA линейна — для нелинейных многообразий лучше t-SNE/UMAP."
                ),
                StudyQuestion(
                    question: "Что такое ранг матрицы и зачем он нужен?",
                    answer: "Ранг — размерность пространства столбцов (= пространства строк). Матрица m×n имеет ранг ≤ min(m, n). Полный ранг — задача обратимости, линейной независимости. В ML: матрица признаков ранга < n — мультиколлинеарность (коллинеарные столбцы), плохо для линейных моделей; низкоранговая структура используется для сжатия и регуляризации.",
                    examples: "Матрица rating (user × item) почти всегда низкоранговая — рекомендации через low-rank matrix factorization: X ≈ U V^T, где U (users × k), V (items × k)."
                )
            ]
        )
    }

    static var probability: StudyTopic {
        StudyTopic(
            title: "Теория вероятностей и статистика",
            summary: "Распределения, MLE, Байес, доверительные интервалы, тесты.",
            questions: [
                StudyQuestion(
                    question: "Теорема Байеса и как её применяют в ML?",
                    answer: "P(A|B) = P(B|A) · P(A) / P(B). Апостериорное = правдоподобие × априорное / нормировка. В ML: Байесовский подход — оценка параметров через апостериорное распределение p(θ|D) ∝ p(D|θ)·p(θ). Позволяет естественно учитывать неопределённость, включать априорную информацию. МАП (Maximum A Posteriori) — мода апостериорного; сводится к MLE при равномерном априорном.",
                    examples: "Наивный Байес для классификации: P(class|features) ∝ P(class) · Π P(feature_i | class) с предположением независимости признаков. Простой, быстрый, хорошо работает для текста (спам-фильтр)."
                ),
                StudyQuestion(
                    question: "Метод максимального правдоподобия (MLE) — в чём суть?",
                    answer: "MLE ищет параметры θ, которые делают наблюдаемые данные наиболее вероятными: θ* = argmax Π p(xᵢ|θ). Обычно максимизируют log-likelihood (сумма вместо произведения). Для нормального распределения — MLE совпадает с выборочным средним и выборочной дисперсией. Связь с кросс-энтропией: минимизация NLL для классификатора = MLE.",
                    examples: "Логистическая регрессия: минимизация log-loss = MLE для модели Бернулли с вероятностью σ(wx). Линейная регрессия с гауссовским шумом: MLE = МНК (минимизация MSE)."
                ),
                StudyQuestion(
                    question: "Смещение и дисперсия (bias-variance tradeoff).",
                    answer: "Ожидаемая ошибка: E[(y - ŷ)²] = Bias(ŷ)² + Var(ŷ) + σ² (неустранимый шум). Модель с малым числом параметров: высокое смещение (underfitting), малая дисперсия. Сложная модель: низкое смещение, высокая дисперсия (overfitting). Цель — баланс. Регуляризация увеличивает смещение, уменьшает дисперсию.",
                    examples: "Полиномиальная регрессия степени 1 — высокое смещение. Степени 20 на 100 точках — высокая дисперсия, модель ловит шум. Кросс-валидация помогает выбрать степень."
                ),
                StudyQuestion(
                    question: "Центральная предельная теорема и её следствия.",
                    answer: "ЦПТ: сумма n независимых одинаково распределённых случайных величин с конечной дисперсией после нормировки (вычитание среднего, деление на √n·σ) сходится по распределению к стандартному нормальному N(0, 1). Это объясняет, почему нормальное распределение везде: выборочное среднее, средняя длина пакетов в сети, шумы измерений.",
                    examples: "Доверительный интервал для среднего: [x̄ - 1.96·s/√n, x̄ + 1.96·s/√n] при больших n — прямое применение ЦПТ."
                ),
                StudyQuestion(
                    question: "Статистические тесты: t-тест, χ², A/B тест.",
                    answer: "t-тест — сравнение средних двух выборок (нормальность, гомоскедастичность). χ²-тест — независимость категориальных переменных или согласие с распределением. A/B-тест: две группы, проверка, превосходит ли новая версия по метрике. p-value — вероятность увидеть результат, если H₀ верна. Порог α (обычно 0.05), Type I / Type II errors, мощность теста.",
                    examples: "A/B-тест CTR кнопки: z-тест для пропорций; минимальный размер выборки считается по ожидаемому эффекту (MDE) и мощности 80%."
                )
            ]
        )
    }

    static var classicalML: StudyTopic {
        StudyTopic(
            title: "Классическое машинное обучение",
            summary: "Регрессия, классификация, SVM, деревья, ансамбли, кластеризация.",
            questions: [
                StudyQuestion(
                    question: "Линейная регрессия: допущения и оценка.",
                    answer: "Модель: y = Xw + ε, ε ~ N(0, σ²). Допущения: 1) линейность, 2) независимость ошибок, 3) гомоскедастичность, 4) нормальность ошибок, 5) отсутствие мультиколлинеарности. Решение МНК: w* = (X^T X)⁻¹ X^T y. Оценка коэффициентов несмещённая, BLUE (Best Linear Unbiased Estimator) по Гаусс–Маркову.",
                    examples: "При мультиколлинеарности X^TX близко к вырожденной, w взрывается → применяют Ridge: w* = (X^TX + λI)⁻¹ X^Ty."
                ),
                StudyQuestion(
                    question: "Логистическая регрессия — модель и функция потерь.",
                    answer: "P(y=1|x) = σ(w^Tx + b), где σ(z) = 1/(1+e^(-z)). Функция потерь — log-loss = -Σ [y log p + (1-y) log(1-p)] = кросс-энтропия. Обучение градиентным спуском (нет закрытой формы). Мультикласс — softmax regression: P(y=k|x) = exp(w_k^T x)/Σ exp(w_j^T x).",
                    examples: "Предсказание оттока клиентов: вероятность ухода + пороги для разных бизнес-решений. Коэффициенты логистической регрессии — логарифмы отношений шансов (log-odds)."
                ),
                StudyQuestion(
                    question: "SVM: идея максимального зазора и ядерный трюк.",
                    answer: "SVM ищет гиперплоскость, максимизирующую зазор между классами. Задача: min ½‖w‖² s.t. yᵢ(w^Txᵢ + b) ≥ 1. Мягкий зазор — допустимы нарушения с штрафом C. Двойственная задача зависит только от скалярных произведений xᵢ^Txⱼ → замена на ядро K(x, x'): RBF (exp(-γ‖x-x'‖²)), полиномиальное. Позволяет нелинейные границы без явного отображения в высокое измерение.",
                    examples: "RBF-SVM на нелинейных данных (два концентрических кольца) идеально разделяет. Гиперпараметры: C (регуляризация), γ (ширина RBF) — подбираются кросс-валидацией."
                ),
                StudyQuestion(
                    question: "Деревья решений, случайный лес, градиентный бустинг — в чём разница?",
                    answer: "Дерево: жадное разбиение по признакам, выбирая splits с максимальным снижением impurity (Gini, entropy) или MSE. Склонно к overfit. Random Forest: независимые деревья на бутстрэп-выборках и случайных подмножествах признаков, ансамбль усредняет → низкая дисперсия. Gradient Boosting: деревья обучаются последовательно, каждое предсказывает остатки предыдущих → низкое смещение. XGBoost/LightGBM/CatBoost — эффективные реализации.",
                    examples: "Табличные данные, средние размеры (10k–10M) — GBM часто превосходит нейросети. Leaderboard Kaggle в 2015–2020 годах был занят LightGBM/XGBoost."
                ),
                StudyQuestion(
                    question: "Метрики классификации: precision, recall, F1, ROC AUC.",
                    answer: "Precision = TP/(TP+FP) — насколько уверен в положительных предсказаниях. Recall = TP/(TP+FN) — сколько истинных положительных пойманы. F1 = гармоническое среднее precision и recall. ROC — кривая TPR vs FPR при варьировании порога; AUC = площадь под ROC, вероятность правильного ранжирования случайной пары (положительный > отрицательный). PR-curve предпочтительнее при сильно несбалансированных классах.",
                    examples: "Медицинский скрининг: высокий recall важнее, даже ценой precision. Фрод-детекция: PR-AUC информативнее ROC-AUC (≈99% примеров не фрод)."
                ),
                StudyQuestion(
                    question: "Кластеризация: K-means, DBSCAN, иерархическая.",
                    answer: "K-means: итеративно присваивает точки ближайшему центроиду и обновляет центроиды; минимизирует внутрикластерное SSE; требует задать k, чувствителен к инициализации (K-means++), находит сферические кластеры. DBSCAN: плотностной — точка ядровая, если в ε-окрестности ≥ minPts; находит произвольные формы, выделяет шум. Иерархическая: agglomerative, строит дендрограмму, можно резать на k.",
                    examples: "Для сегментации клиентов — K-means на стандартизованных признаках; elbow method или silhouette для выбора k. DBSCAN для обнаружения аномалий."
                )
            ]
        )
    }

    static var neuralNetworks: StudyTopic {
        StudyTopic(
            title: "Нейронные сети: основы",
            summary: "Перцептрон, MLP, активации, backprop, инициализация, регуляризация.",
            questions: [
                StudyQuestion(
                    question: "Как устроен многослойный перцептрон (MLP)?",
                    answer: "MLP: последовательность слоёв, каждый — аффинное преобразование y = Wx + b + нелинейная активация (ReLU, sigmoid, tanh). Теорема универсальной аппроксимации: MLP с одним скрытым слоем может сколь угодно точно приблизить любую непрерывную функцию на компакте (при достаточной ширине). Глубокие сети достигают этого с меньшим числом параметров за счёт композиционности.",
                    examples: "MLP [784, 128, 10] для MNIST: 784·128 + 128·10 + смещения ≈ 101k параметров. Современные трансформеры — сотни миллиардов."
                ),
                StudyQuestion(
                    question: "Backpropagation — как считается градиент?",
                    answer: "Backprop = применение правила цепочки к вычислительному графу. Forward pass: вычисляем все промежуточные значения. Backward pass: начиная с loss, распространяем ∂L/∂output назад по графу, используя локальные производные узлов. Для слоя y = Wx: ∂L/∂W = ∂L/∂y · x^T, ∂L/∂x = W^T · ∂L/∂y. Autodiff во фреймворках (PyTorch, JAX) строит граф и применяет reverse-mode AD.",
                    examples: "PyTorch: loss.backward() автоматически проходит граф; .grad каждого parameter содержит градиент. Оптимизатор применяет шаг: w -= lr · grad."
                ),
                StudyQuestion(
                    question: "Функции активации: ReLU, sigmoid, tanh, GELU.",
                    answer: "Sigmoid σ(z) = 1/(1+e^(-z)): (0,1), насыщается → vanishing gradient. Tanh: (-1,1), центрированный, но тоже насыщается. ReLU(z) = max(0, z): не насыщается в положительной зоне, простая, но «dying ReLU» при z<0. Leaky ReLU, GELU, SiLU/Swish — гладкие альтернативы. GELU (z · Φ(z)) — стандарт в трансформерах.",
                    examples: "ResNet, VGG — ReLU. BERT, GPT — GELU. В выходном слое: softmax (мультикласс), sigmoid (бинарная), linear (регрессия)."
                ),
                StudyQuestion(
                    question: "Затухание и взрыв градиентов — причины и решения.",
                    answer: "В глубоких сетях градиент на ранних слоях = произведение многих якобианов. Если их спектральный радиус <1 — градиент затухает; >1 — взрывается. Решения: 1) ReLU вместо sigmoid, 2) правильная инициализация (He для ReLU, Xavier для tanh), 3) residual connections (ResNet), 4) нормализация слоёв (BatchNorm, LayerNorm), 5) gradient clipping при взрыве, 6) LSTM/GRU вместо ванильных RNN.",
                    examples: "ResNet-152 работает за счёт skip connections: y = F(x) + x, градиент сохраняется через идентичный путь."
                ),
                StudyQuestion(
                    question: "Регуляризация нейросетей: dropout, BatchNorm, weight decay.",
                    answer: "Dropout: при обучении случайно обнуляет доли p нейронов, заставляет сеть не полагаться на отдельные нейроны; при inference выключен (умножение на 1-p). BatchNorm: нормирует активации в батче (μ, σ по батчу, learnable γ, β); стабилизирует обучение, позволяет больший lr. LayerNorm — по признакам (лучше для RNN/Transformer). Weight decay = L² на веса, эквивалентен Ridge.",
                    examples: "Ванильный трансформер: LayerNorm + dropout + weight decay (AdamW). Для CNN — BatchNorm стандарт."
                )
            ]
        )
    }

    static var optimization: StudyTopic {
        StudyTopic(
            title: "Оптимизация в ML",
            summary: "Градиентный спуск, momentum, Adam, планировщики LR.",
            questions: [
                StudyQuestion(
                    question: "Градиентный спуск: batch, mini-batch, SGD — разница.",
                    answer: "Batch GD: градиент по всей выборке — точный, но медленный. SGD: градиент по одному примеру — шумный, может выскакивать из локальных минимумов, медленнее из-за накладных расходов. Mini-batch (32–512) — золотая середина: GPU эффективен, стохастичность помогает, градиент достаточно гладкий. Время эпохи ≈ batch_GD, но шаги чаще.",
                    examples: "Transformers обучаются на батчах 4k–1M токенов с gradient accumulation (много мини-батчей складываются, шаг раз в N)."
                ),
                StudyQuestion(
                    question: "Что делает momentum и как работают Adam, RMSprop?",
                    answer: "Momentum: v = β·v + g, w -= lr·v — накопленная инерция направлений, гасит осцилляции, ускоряет в плоских овражных ландшафтах. RMSprop: делит шаг на скользящее среднее квадрата градиента → adaptive learning rate per-parameter. Adam = momentum + RMSprop + bias correction. AdamW — Adam с правильной реализацией weight decay (не через gradient, а напрямую к весам).",
                    examples: "Transformers: AdamW с β1=0.9, β2=0.95/0.98, warmup + cosine decay. CNN (ResNet): SGD+momentum часто даёт лучшее качество, чем Adam."
                ),
                StudyQuestion(
                    question: "Learning rate: warmup, schedulers, почему важен.",
                    answer: "Слишком большой lr → расходимость; слишком маленький → застревание, долгое обучение. Warmup: линейно растёт от 0 до lr за первые N шагов — важен для AdamW в трансформерах. Планировщики: step decay, exponential, cosine annealing, one-cycle. Общая стратегия: высокий lr в начале (исследование), низкий в конце (уточнение минимума).",
                    examples: "Transformers: warmup 10k шагов, cosine до 10% от пика. ResNet: step decay ×0.1 каждые 30 эпох."
                ),
                StudyQuestion(
                    question: "Проблемы оптимизационного ландшафта в глубоких сетях.",
                    answer: "Невыпуклый, много локальных минимумов и седловых точек. Эмпирически: большинство «локальных минимумов» — похожие по качеству (Goodfellow, Choromanska). Седловые точки замедляют GD, но SGD их проходит благодаря шуму. Плоские минимумы обобщают лучше острых (theoretically debated, practically observed). Loss landscape visualisation (Li et al., 2018) — полезный инструмент анализа.",
                    examples: "Linear Mode Connectivity: между хорошими минимумами часто есть путь без сильных потерь — обнаружение через usually SGD trajectories."
                ),
                StudyQuestion(
                    question: "Стохастический градиент — почему работает как регуляризатор?",
                    answer: "Шум SGD мешает сходимости к острым минимумам (они чувствительны к шуму), предпочитая плоские — у плоских лучше генерализация по теории MDL/PAC-Bayes. Это подтверждается эмпирически: маленький batch часто даёт лучшее обобщение, чем большой (при прочих равных). Large-batch training требует корректировки: scaling rule (lr ~ batch), LARS/LAMB оптимизаторы.",
                    examples: "ResNet-50 + 8k batch: без warmup lr scaling не сходится. Goyal et al. (2017) — линейное правило + warmup дают 1-hour ImageNet."
                )
            ]
        )
    }

    static var deepLearning: StudyTopic {
        StudyTopic(
            title: "Современный deep learning: CNN, RNN, Transformer",
            summary: "Свёрточные для CV, рекуррентные для последовательностей, трансформеры.",
            questions: [
                StudyQuestion(
                    question: "CNN: свёртка, пулинг, почему работает для изображений.",
                    answer: "Свёртка: окно (kernel) скользит по входу, вычисляя скалярное произведение; один и тот же kernel разделяет параметры по пространству → translation equivariance. Пулинг (max/avg) уменьшает разрешение, даёт частичную инвариантность к сдвигу. Последовательность conv+activation+pool строит иерархию признаков: низкие слои — края, средние — текстуры, высокие — объекты.",
                    examples: "VGG: 3×3 conv + ReLU + 2×2 max pool. ResNet: bottleneck 1×1→3×3→1×1 + skip. EfficientNet: compound scaling — глубина, ширина, разрешение вместе."
                ),
                StudyQuestion(
                    question: "Архитектура Transformer: self-attention, positional encoding.",
                    answer: "Self-attention: для каждого токена вычисляется Q, K, V (линейные проекции); attention = softmax(QK^T/√d) · V. Механизм позволяет любому токену «смотреть» на любой другой, в отличие от RNN. Multi-head: h параллельных attention с разных подпространств. Positional encoding добавляет информацию о позиции (абсолютные sinusoidal, относительные, RoPE). Блок: attention + FFN (2 linear + activation), оба с residual и LayerNorm.",
                    examples: "BERT — encoder-only, GPT — decoder-only (причинная маска), T5/BART — encoder-decoder. Современный LLM — decoder-only с RoPE, RMSNorm, SwiGLU."
                ),
                StudyQuestion(
                    question: "RNN, LSTM, GRU: разница и применение.",
                    answer: "RNN: h_t = tanh(W_h h_{t-1} + W_x x_t); страдает vanishing gradient на длинных последовательностях. LSTM: ячейка памяти c_t + 3 гейта (forget, input, output) — селективно обновляет/забывает. GRU: упрощённый LSTM с 2 гейтами (update, reset), меньше параметров. Сейчас почти вытеснены трансформерами, но ещё встречаются в low-resource и онлайн-задачах.",
                    examples: "Mamba/S4 (state-space models) возрождают идею RNN с линейной сложностью по длине — конкурируют с трансформерами на длинных последовательностях."
                ),
                StudyQuestion(
                    question: "Как обучать большие модели: distributed training, mixed precision.",
                    answer: "Data parallelism: копия модели на каждом GPU, разные батчи, all-reduce градиентов. Model parallelism: модель разрезана по слоям (pipeline) или по ширине (tensor parallel, Megatron). ZeRO (DeepSpeed) шардирует состояния оптимизатора/градиенты/параметры. Mixed precision (fp16/bf16): вдвое меньше памяти, tensor cores. Gradient checkpointing: пересчёт активаций при backprop ↓память, ↑время.",
                    examples: "GPT-3 175B: 3D parallelism (data + pipeline + tensor). Llama 70B: тот же подход, но больше публичных отчётов."
                ),
                StudyQuestion(
                    question: "Предобучение и fine-tuning, LoRA, RLHF.",
                    answer: "Предобучение: self-supervised loss на огромном корпусе (MLM для BERT, next-token для GPT). Fine-tuning: supervised adaptation на целевой задаче. LoRA: замораживаем базовую модель, добавляем низкоранговые adapters A·B — обучаем ~1% параметров. RLHF: SFT на демонстрациях → reward model на человеческих предпочтениях → PPO оптимизирует политику против reward. Современная альтернатива — DPO (без отдельной reward model).",
                    examples: "Llama 2 Chat: SFT → RLHF с помощью ~1M сравнений. Большинство open-source моделей дообучают через QLoRA (quantized base + LoRA) на одном GPU."
                )
            ]
        )
    }
}
