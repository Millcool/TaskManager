import Foundation

enum StudyContent_CS {
    static func makeGroup() -> StudyGroup {
        StudyGroup(
            title: "Информатика и компьютерные науки",
            subtitle: "Алгоритмы, структуры данных, БД, ОС, сети, теория вычислений",
            iconSystemName: "cpu",
            topics: [algorithms, dataStructures, databases, operatingSystems, networks, theoryOfComputation]
        )
    }

    static var algorithms: StudyTopic {
        StudyTopic(
            title: "Алгоритмы и сложность",
            summary: "Асимптотическая сложность, сортировки, поиск, жадные алгоритмы, динамическое программирование, алгоритмы на графах.",
            questions: [
                StudyQuestion(
                    question: "Что такое асимптотическая сложность, как определяются O, Θ, Ω?",
                    answer: "Асимптотика описывает поведение функции T(n) при n→∞. Запись O(g(n)) — это множество функций f, для которых существуют c>0 и n₀, такие что для всех n≥n₀ выполнено f(n)≤c·g(n) (верхняя оценка). Ω(g(n)) — нижняя оценка (f(n)≥c·g(n)). Θ(g(n)) = O(g(n)) ∩ Ω(g(n)) — точная оценка. На практике O(·) — худший случай, Θ(·) — когда нижняя и верхняя оценки совпадают.",
                    examples: "Сортировка слиянием: T(n)=2T(n/2)+Θ(n) → T(n)=Θ(n log n) по мастер-теореме. Линейный поиск: O(n) в худшем, Ω(1) в лучшем, Θ(n) в среднем."
                ),
                StudyQuestion(
                    question: "Сравните сортировки: быструю, слиянием, пирамидальную.",
                    answer: "Быстрая сортировка (Quicksort): среднее Θ(n log n), худший O(n²) при плохом выборе опорного; in-place O(log n) стека; неустойчива. Сортировка слиянием (Mergesort): Θ(n log n) во всех случаях, устойчива, требует O(n) доп. памяти. Пирамидальная (Heapsort): Θ(n log n) гарантированно, in-place, неустойчива, медленнее на практике из-за cache misses. Для средних данных Quicksort быстрее, для гарантий — Mergesort/Heapsort.",
                    examples: "TimSort (Python/Java) — адаптивный Mergesort; std::sort в C++ — Introsort (Quick + Heap при глубине > 2 log n)."
                ),
                StudyQuestion(
                    question: "Как работает динамическое программирование и когда его применять?",
                    answer: "ДП применяется, когда задача имеет 1) оптимальную подструктуру (решение строится из решений подзадач) и 2) перекрывающиеся подзадачи (одни и те же подзадачи встречаются многократно). Идея — запомнить (memoize) результаты. Два стиля: top-down с рекурсией+мемоизацией, bottom-up с таблицей. Сложность = число состояний × время перехода.",
                    examples: "Наибольшая общая подпоследовательность (LCS): dp[i][j] = dp[i-1][j-1]+1 если a[i]=b[j], иначе max(dp[i-1][j], dp[i][j-1]). O(n·m) памяти и времени. Рюкзак 0/1: dp[i][w] = max(dp[i-1][w], dp[i-1][w-wi]+vi)."
                ),
                StudyQuestion(
                    question: "Опишите алгоритмы поиска кратчайших путей в графе.",
                    answer: "BFS — невзвешенный граф, O(V+E). Дейкстра — неотрицательные веса, на приоритетной очереди O((V+E)log V) или O(E + V log V) с Фибоначчиевой кучей. Беллман–Форд — любые веса без отрицательных циклов, O(V·E), умеет детектировать отрицательные циклы. Флойд–Уоршелл — все пары, O(V³). A* — Дейкстра с эвристикой, оптимален при допустимой эвристике.",
                    examples: "Дейкстра: priority queue (dist, v); на каждой итерации достаём минимум, релаксируем соседей. Беллман–Форд: V-1 итераций релаксации всех рёбер; V-я итерация с изменением ⇒ отрицательный цикл."
                ),
                StudyQuestion(
                    question: "Что такое NP-полнота и какие задачи к ней относятся?",
                    answer: "P — задачи разрешимы за полиномиальное время. NP — задачи, решение которых проверяется за полиномиальное время. NP-полная задача: принадлежит NP и к ней полиномиально сводится любая задача из NP. Если NP-полную задачу решить за P, то P=NP. Гипотеза P≠NP не доказана. NP-hard — задачи, к которым сводится NP-полная (могут быть не в NP).",
                    examples: "SAT (задача выполнимости булевых формул) — первая доказанная NP-полная (теорема Кука–Левина). Другие: 3-SAT, клика, гамильтонов цикл, раскраска графа, рюкзак (в десятичных), коммивояжёр."
                ),
                StudyQuestion(
                    question: "Жадные алгоритмы: когда работают, как доказать корректность?",
                    answer: "Жадный алгоритм делает на каждом шаге локально оптимальный выбор. Работает, когда задача обладает свойством жадного выбора (локальный оптимум → глобальный) и оптимальной подструктурой. Доказательство корректности: 1) аргумент «обмена» (exchange argument) — показать, что оптимальное решение можно преобразовать в жадное без ухудшения; 2) индукция по шагам.",
                    examples: "Коды Хаффмана (строим дерево от наименьших частот). Задача о выборе мероприятий (сортируем по окончанию). Минимальное остовное дерево: Прим и Крускал жадно добавляют рёбра."
                )
            ]
        )
    }

    static var dataStructures: StudyTopic {
        StudyTopic(
            title: "Структуры данных",
            summary: "Массивы, списки, стеки, очереди, деревья, хеш-таблицы, кучи, сбалансированные деревья.",
            questions: [
                StudyQuestion(
                    question: "Сравните массив и связный список.",
                    answer: "Массив: непрерывная память, доступ по индексу O(1), вставка/удаление в середине O(n), отличная локальность кеша. Связный список: узлы в произвольных местах памяти, доступ по индексу O(n), вставка/удаление в известной позиции O(1), плохая локальность, накладные расходы на указатели. На практике массивы почти всегда быстрее из-за кеша.",
                    examples: "std::vector vs std::list в C++: vector почти всегда побеждает, даже при частых вставках/удалениях, если элементы переиспользуются."
                ),
                StudyQuestion(
                    question: "Как устроена хеш-таблица и каковы её гарантии?",
                    answer: "Хеш-таблица: массив «корзин» (buckets), ключ хешируется h(key) mod N. Коллизии разрешаются: 1) цепочками (каждый bucket — список), 2) открытой адресацией (линейное/квадратичное пробирование). Амортизированное O(1) для вставки/поиска/удаления при хорошей хеш-функции и коэффициенте заполнения ≤ 0.7–0.75. При переполнении — rehash: создаём таблицу в 2 раза больше, переносим элементы. Худший случай — O(n) при плохом хеше.",
                    examples: "HashMap в Java использует цепочки + деревья (при длине > 8) для защиты от hash collision attacks. Python dict — открытая адресация."
                ),
                StudyQuestion(
                    question: "Двоичная куча, её операции и применения.",
                    answer: "Двоичная куча — почти полное бинарное дерево, хранимое в массиве (родитель i/2, дети 2i и 2i+1). Свойство: ключ родителя ≥ (max-heap) или ≤ (min-heap) ключей детей. Операции: insert — добавить в конец + sift up O(log n); extract-max — заменить корень последним + sift down O(log n); build — сверху вниз за O(n). Применение: priority queue, heapsort, алгоритм Дейкстры, top-k.",
                    examples: "Top-k: поддерживаем min-heap размера k, для каждого элемента: если больше минимума — заменяем и сифтим вниз. Итого O(n log k)."
                ),
                StudyQuestion(
                    question: "Сбалансированные деревья поиска: AVL, красно-чёрные.",
                    answer: "BST гарантирует O(log n) только при сбалансированности, иначе — O(n). AVL: разница высот поддеревьев ≤ 1, ребалансировка через повороты (left, right, LR, RL); высота ≤ 1.44 log n. Красно-чёрное дерево: узлы крашены так, что 1) корень чёрный, 2) красный → оба ребёнка чёрные, 3) на каждом пути одинаковое число чёрных; высота ≤ 2 log(n+1). AVL строже сбалансировано (быстрее чтения), RB — дешевле вставки/удаления.",
                    examples: "std::map/set в C++ — красно-чёрное дерево. TreeMap/TreeSet в Java тоже. AVL — в СУБД для индексов."
                ),
                StudyQuestion(
                    question: "Union-Find: реализация и сложность.",
                    answer: "Union-Find (DSU) хранит разбиение множества на непересекающиеся подмножества. Операции: find(x) — представитель множества, union(x,y) — объединить. Оптимизации: сжатие путей (path compression) и объединение по рангу/размеру. Амортизированная сложность — O(α(n)) на операцию, где α — обратная функция Аккермана (практически константа).",
                    examples: "Применение: алгоритм Крускала для MST — перебираем рёбра по возрастанию веса, добавляем если разные компоненты; офлайн-LCA; детектирование циклов."
                ),
                StudyQuestion(
                    question: "Что такое префиксное дерево (trie) и когда его использовать?",
                    answer: "Trie — дерево, где каждое ребро помечено символом, а пути от корня задают префиксы. Узел отмечен, если соответствует полному слову. Вставка/поиск — O(|s|). Плюсы: быстрый поиск по префиксу, автодополнение, проверка вхождения в словарь. Минусы: много памяти. Оптимизации: сжатый trie (radix tree), suffix automaton.",
                    examples: "Автодополнение в поисковиках. Алгоритм Ахо–Корасик (trie + связи неудач) для поиска множества паттернов в тексте за O(n+m+z)."
                )
            ]
        )
    }

    static var databases: StudyTopic {
        StudyTopic(
            title: "Базы данных",
            summary: "Реляционная модель, нормализация, SQL, индексы, транзакции и свойства ACID.",
            questions: [
                StudyQuestion(
                    question: "Что такое нормальные формы и зачем они нужны?",
                    answer: "Нормализация — процесс декомпозиции таблиц для устранения избыточности и аномалий. 1НФ: атомарные значения. 2НФ: 1НФ + отсутствие частичной зависимости неключевых атрибутов от части составного ключа. 3НФ: 2НФ + отсутствие транзитивной зависимости. НФБК (Бойса–Кодда): все функциональные зависимости исходят от суперключа. Денормализация применяется ради производительности чтения.",
                    examples: "Таблица (студент, группа, куратор): куратор зависит от группы (транзитивно через группу). Это нарушение 3НФ — выделяем отдельную таблицу (группа, куратор)."
                ),
                StudyQuestion(
                    question: "ACID — что это и как реализуется?",
                    answer: "Atomicity: транзакция выполняется целиком или не выполняется вовсе (WAL, rollback). Consistency: БД переходит из одного корректного состояния в другое (constraints). Isolation: параллельные транзакции не мешают друг другу (уровни: read uncommitted, read committed, repeatable read, serializable). Durability: зафиксированные изменения сохраняются при сбоях (WAL+fsync). Типичные механизмы: журнал упреждающей записи, MVCC, блокировки 2PL.",
                    examples: "PostgreSQL — MVCC: каждая транзакция видит снимок данных. Repeatable Read в PG = Snapshot Isolation (сильнее стандарта)."
                ),
                StudyQuestion(
                    question: "Индексы: B-дерево, хеш, обратный. Когда что использовать?",
                    answer: "B-tree (чаще B+-tree) — основной индекс: диапазоны, равенство, сортировка; O(log_B n). Hash-индекс — только равенство, O(1) средне. Bitmap — для колонок с малой кардинальностью. GiST/GIN (PostgreSQL) — полнотекстовый поиск, массивы, геометрия. Covering index включает все нужные колонки и позволяет не обращаться к таблице. Избыточные индексы замедляют write и стоят памяти.",
                    examples: "CREATE INDEX idx ON orders(customer_id, created_at DESC) — диапазонные запросы по клиенту и дате. EXPLAIN ANALYZE покажет, использовался ли индекс."
                ),
                StudyQuestion(
                    question: "Уровни изоляции транзакций и аномалии.",
                    answer: "Read Uncommitted: видны незафиксированные изменения (dirty read). Read Committed: dirty read исключён, но возможен non-repeatable read (повтор select даёт другие данные). Repeatable Read: дважды read даёт одно и то же; возможны phantoms (новые строки в диапазоне). Serializable: полная эквивалентность последовательному исполнению. Snapshot Isolation — отдельная модель MVCC. Чем выше — тем меньше аномалий, но больше накладных расходов.",
                    examples: "Финансы: Serializable для перевода денег. Читательские запросы — Read Committed, достаточно для большинства случаев."
                ),
                StudyQuestion(
                    question: "Что такое CAP и BASE?",
                    answer: "CAP-теорема (Brewer): распределённая система может гарантировать только 2 из 3 свойств — Consistency (линеаризуемость), Availability (ответ на каждый запрос), Partition tolerance (устойчивость к разделению сети). При сетевом разделении выбираем между C и A. BASE: Basically Available, Soft state, Eventually consistent — компромисс для высокодоступных систем.",
                    examples: "CP: HBase, MongoDB (по умолчанию). AP: Cassandra, DynamoDB. При split-brain CP-системы могут отказать в ответе, AP — отдать устаревшие данные."
                ),
                StudyQuestion(
                    question: "Оптимизация SQL-запросов: что смотреть и что исправлять?",
                    answer: "1) EXPLAIN/EXPLAIN ANALYZE — видеть план (seq scan, index scan, join type). 2) Проверить индексы: покрывают ли WHERE, JOIN, ORDER BY. 3) Избегать SELECT *, функций над индексируемыми колонками в WHERE. 4) JOIN: hash join для больших несортированных, merge join — для отсортированных, nested loop — для маленьких. 5) Статистика (ANALYZE) актуальна. 6) Дорогие подзапросы превратить в JOIN или CTE. 7) Пагинация по keyset (WHERE id > ...), а не OFFSET.",
                    examples: "WHERE date_trunc('day', created_at) = CURRENT_DATE — индекс по created_at не используется; заменить на created_at >= today AND created_at < tomorrow."
                )
            ]
        )
    }

    static var operatingSystems: StudyTopic {
        StudyTopic(
            title: "Операционные системы",
            summary: "Процессы и потоки, планирование, виртуальная память, файловые системы, синхронизация.",
            questions: [
                StudyQuestion(
                    question: "Процесс vs поток: различия и когда что использовать.",
                    answer: "Процесс — изолированный контейнер с собственным адресным пространством, файловыми дескрипторами, идентификатором. Поток — единица исполнения внутри процесса, делит адресное пространство и ресурсы с другими потоками. Переключение контекста между потоками дешевле, коммуникация через общую память. Но потоки должны синхронизироваться (мьютексы, атомики). Процессы — для изоляции (браузерные вкладки), потоки — для параллелизма с общими данными.",
                    examples: "Chrome: отдельный процесс на вкладку — краш не валит браузер. Веб-сервер: пул потоков, каждый поток обрабатывает запрос, общий кеш."
                ),
                StudyQuestion(
                    question: "Алгоритмы планирования процессов.",
                    answer: "FCFS: первый пришёл — первый обслужен, проста, но starve короткие при длинных. SJF (Shortest Job First): минимизирует среднее время ожидания, требует знания длин. Round Robin: квант времени, справедливо. Priority scheduling: по приоритету, риск голодания → aging. Multilevel feedback queue (Linux CFS использует аналог): несколько очередей с разными квантами, адаптируется к типу задачи (CPU-bound vs I/O-bound).",
                    examples: "Linux CFS: идея «виртуального времени», самый «отставший» поток выбирается следующим. Реальный диспетчер — красно-чёрное дерево по vruntime."
                ),
                StudyQuestion(
                    question: "Как работает виртуальная память?",
                    answer: "Адреса, которые видит процесс (виртуальные), транслируются в физические через MMU и таблицы страниц. Страница ~4 КБ. Уровневая таблица страниц (x86-64 — 4-5 уровней) экономит память. TLB кеширует переводы. При обращении к отсутствующей странице — page fault, ОС подгружает с диска (swap/mmap). Это даёт изоляцию процессов, overcommit, файлы в памяти (mmap).",
                    examples: "Copy-on-write при fork(): родитель и ребёнок делят физические страницы, запись → копирование. Пустая виртуальная память при старте занимает минимум физической."
                ),
                StudyQuestion(
                    question: "Синхронизация: мьютекс, семафор, монитор, атомики.",
                    answer: "Мьютекс: взаимное исключение, lock/unlock. Семафор: счётчик ресурсов, P/V (wait/signal), двоичный семафор ≈ мьютекс. Монитор: объект с гарантированным взаимным исключением методов + condition variables (wait/notify). Атомики: операции CAS, fetch_add — без блокировок, для счётчиков и lock-free структур. Ошибки: deadlock (4 условия Коффмана), livelock, race condition, priority inversion.",
                    examples: "Классика: обедающие философы (5 философов, 5 вилок, блокировки по кругу → deadlock). Решение: асимметрия, монитор, резервирование обеих вилок атомарно."
                ),
                StudyQuestion(
                    question: "Файловые системы: inode, журналирование, что делать при сбое.",
                    answer: "Inode хранит метаданные файла: тип, права, владельца, размер, указатели на блоки данных. Имя файла — в записи директории, которая указывает на inode. Журналируемые ФС (ext3/4, NTFS) ведут журнал операций: запись в журнал → применение → отметка о завершении. При сбое — проигрывание журнала. COW ФС (ZFS, Btrfs) пишут новые блоки, старые остаются до фиксации — атомарность без отдельного журнала.",
                    examples: "rm на Linux удаляет запись в директории; inode освобождается только когда ни один процесс не держит открытый fd. Потому можно удалить работающий исполняемый файл."
                ),
                StudyQuestion(
                    question: "I/O: блокирующий, неблокирующий, async, epoll/kqueue/io_uring.",
                    answer: "Блокирующий I/O: поток ждёт результат, простая модель, но не масштабируется. Неблокирующий: read возвращает EWOULDBLOCK, приходится опрашивать. Мультиплексирование — select/poll/epoll (Linux)/kqueue (BSD/Darwin) — ждать готовности сразу многих fd без отдельных потоков. io_uring (Linux ≥ 5.1) — асинхронные очереди в shared memory, минимум syscall. Модель reactor (Nginx, Node.js) — один поток на epoll_wait.",
                    examples: "Node.js: event loop + thread pool для блокирующих операций (fs). Go: goroutines мультиплексируются на потоки, runtime под капотом использует epoll/kqueue."
                )
            ]
        )
    }

    static var networks: StudyTopic {
        StudyTopic(
            title: "Компьютерные сети",
            summary: "Модели OSI и TCP/IP, маршрутизация, TCP/UDP, DNS, HTTP, TLS.",
            questions: [
                StudyQuestion(
                    question: "Что такое модель OSI и как она соотносится с TCP/IP?",
                    answer: "OSI — 7 уровней: физический, канальный, сетевой, транспортный, сеансовый, представления, приложений. TCP/IP — практическая 4-уровневая: link (Ethernet), internet (IP), transport (TCP/UDP), application (HTTP, DNS). Разделение ответственности: каждый уровень предоставляет сервис верхнему. Инкапсуляция: данные → сегмент (TCP) → пакет (IP) → кадр (Ethernet).",
                    examples: "HTTP-запрос: браузер → HTTP → TLS → TCP → IP → Ethernet → физический сигнал. На сервере обратный путь."
                ),
                StudyQuestion(
                    question: "TCP vs UDP: когда что использовать?",
                    answer: "TCP: соединение (3-way handshake), надёжность (acks, повторы), упорядочение, контроль потока (flow control) и перегрузки (congestion control). Накладные расходы на заголовок и RTT. UDP: без соединения, без гарантий доставки и порядка, минимальный заголовок, быстро. TCP — для HTTP, SSH, БД; UDP — для DNS, видео/аудио реального времени, игр. QUIC — UDP + собственная надёжность + TLS 1.3, быстрее TCP при пакетных потерях.",
                    examples: "HTTP/3 = QUIC + HTTP: multipath, без head-of-line blocking как в HTTP/2-over-TCP."
                ),
                StudyQuestion(
                    question: "Как работает TCP-рукопожатие и контроль перегрузки?",
                    answer: "3-way handshake: клиент SYN(seq=x), сервер SYN-ACK(seq=y, ack=x+1), клиент ACK(ack=y+1). Завершение: FIN/ACK с обеих сторон. Контроль перегрузки: slow start (cwnd удваивается каждый RTT до ssthresh), congestion avoidance (линейный рост), fast retransmit при 3 dup ACKs, fast recovery. Алгоритмы: Reno, CUBIC (Linux по умолчанию), BBR (Google — модель пропускной способности и RTT).",
                    examples: "При потере пакета Reno: cwnd/=2; CUBIC использует кубическую функцию. BBR пишет полоса × rtt → лучше на высоких задержках."
                ),
                StudyQuestion(
                    question: "DNS: как резолвится имя и что такое TTL, рекурсия.",
                    answer: "Клиент → локальный резолвер (рекурсивный). Резолвер: корневой сервер → TLD (.com) → авторитетный для домена → IP. Кеширует ответы с TTL. Типы записей: A (IPv4), AAAA (IPv6), CNAME (алиас), MX (почта), TXT (SPF/DKIM), NS (делегирование). DNSSEC — подпись записей; DoH/DoT — DNS через HTTPS/TLS.",
                    examples: "dig +trace example.com покажет путь от корневых. dnsmasq или systemd-resolved кешируют локально."
                ),
                StudyQuestion(
                    question: "HTTPS и TLS: как устанавливается защищённое соединение?",
                    answer: "TLS 1.3 handshake: клиент шлёт ClientHello (supported ciphers, key share), сервер — ServerHello (выбранный cipher, key share, сертификат подписан приватным ключом). Клиент проверяет цепочку сертификатов до корневого CA. Общий сеансовый ключ выводится из ECDHE. Далее — симметричное шифрование (AES-GCM/ChaCha20). TLS 1.3 — 1-RTT (или 0-RTT для resumption), убрал устаревшие шифры.",
                    examples: "Forward secrecy: даже если приватный ключ сервера утечёт, прошлые сессии нельзя расшифровать — ECDHE эфемерный. Let's Encrypt выдаёт бесплатные сертификаты через ACME."
                ),
                StudyQuestion(
                    question: "NAT, CIDR, маршрутизация — базовый обзор.",
                    answer: "IPv4-адрес 32 бит. CIDR: запись /n означает n бит префикса сети (например, 192.168.1.0/24 = 256 адресов). NAT трансформирует приватные адреса (10.0.0.0/8, 192.168.0.0/16) в публичный. Маршрутизация: таблица маршрутов (destination, gateway, interface); longest prefix match. BGP — межавтономная, OSPF/IS-IS — внутри AS. IPv6 убирает NAT за счёт 128-битных адресов.",
                    examples: "При curl: маршрут 0.0.0.0/0 via дефолтный шлюз → ARP ищет MAC шлюза → Ethernet-кадр. NAT подменяет src IP и порт, хранит mapping."
                )
            ]
        )
    }

    static var theoryOfComputation: StudyTopic {
        StudyTopic(
            title: "Теория вычислений и формальные языки",
            summary: "Автоматы, грамматики, машины Тьюринга, разрешимость, вычислительная сложность.",
            questions: [
                StudyQuestion(
                    question: "Иерархия Хомского: какие классы языков и автоматов?",
                    answer: "Тип 3 — регулярные: конечные автоматы (DFA/NFA), регулярные выражения. Тип 2 — контекстно-свободные (CFG): распознаются магазинными автоматами. Тип 1 — контекстно-зависимые: линейно ограниченные автоматы. Тип 0 — рекурсивно перечислимые: машины Тьюринга. Включения строгие: {aⁿbⁿ} — КС, не регулярный; {aⁿbⁿcⁿ} — КЗ, не КС.",
                    examples: "Лексер языка — регулярный (ключевые слова, идентификаторы). Парсер — КС (вложенные скобки, рекурсивная грамматика). Проверка типов — уже Тьюринг-полна в языках с полноценной системой типов."
                ),
                StudyQuestion(
                    question: "Что такое машина Тьюринга и тезис Чёрча–Тьюринга?",
                    answer: "Машина Тьюринга — абстрактная модель: лента с ячейками, головка чтения-записи, конечное управление (множество состояний), переходы (состояние, символ) → (новое состояние, символ, движение). Тезис Чёрча–Тьюринга: всё, что интуитивно вычислимо, вычислимо на МТ (и эквивалентно λ-исчислению, μ-рекурсивным функциям, современным ЯП). Это не теорема, а эмпирическое утверждение.",
                    examples: "Brainfuck — Тьюринг-полный минимальный язык (8 команд). Game of Life — Тьюринг-полная клеточная автоматика. Шаблоны C++ — Тьюринг-полны."
                ),
                StudyQuestion(
                    question: "Теорема Райса и неразрешимые задачи.",
                    answer: "Теорема Райса: любое нетривиальное семантическое свойство распознавателя (программы как функции) неразрешимо. Halting Problem: нет алгоритма, определяющего по произвольной программе и входу, остановится ли она. Доказательство — диагональное: если бы H(p, x) решал, строим программу D(p) = H(p,p)? loop : stop, получаем противоречие для D(D).",
                    examples: "Практические следствия: статическая проверка «всегда завершается», «не обращается к памяти вне границ» в общем случае неразрешимы. Поэтому статические анализаторы используют приближения (sound или complete, не оба)."
                ),
                StudyQuestion(
                    question: "Классы сложности: P, NP, PSPACE, EXPTIME.",
                    answer: "P — полиномиальное время. NP — полиномиальная проверка сертификата. co-NP — проверка опровержения. PSPACE — полиномиальная память (включает NP). EXPTIME — экспоненциальное время. Цепочка: P ⊆ NP ⊆ PSPACE ⊆ EXPTIME; P ⊊ EXPTIME доказано (теорема о временной иерархии), остальные строгие включения — открытые вопросы.",
                    examples: "TQBF (истинность кванторной булевой формулы) — PSPACE-полная. Шахматы на доске n×n — EXPTIME-полные."
                ),
                StudyQuestion(
                    question: "NFA vs DFA: эквивалентность и трансформация.",
                    answer: "DFA: по состоянию и символу — однозначно следующее состояние. NFA: возможно несколько переходов, ε-переходы. Эквивалентность по выразительной мощности: любой NFA можно преобразовать в DFA методом построения подмножеств (subset construction) — состояния нового DFA — подмножества состояний NFA. Число состояний может вырасти экспоненциально (до 2ⁿ).",
                    examples: "Regex → NFA (Томпсон) → DFA (subset) → минимизация (алгоритм Хопкрофта, O(n log n))."
                ),
                StudyQuestion(
                    question: "Лемма о накачке для регулярных и КС языков.",
                    answer: "Для регулярных: если L регулярен, существует p такое, что для любого w ∈ L, |w| ≥ p, w = xyz, |xy| ≤ p, |y| ≥ 1, и xyⁱz ∈ L для всех i ≥ 0. Используется для доказательства нерегулярности. Для КС-языков — аналогично, с разбиением на 5 частей. Методом от противного: допустим L регулярен, накачиваем, получаем строку вне L.",
                    examples: "{aⁿbⁿ}: допустим регулярен, берём w = aᵖbᵖ; тогда y содержит только a (т.к. |xy|≤p), накачиваем y² — a становится больше, чем b — противоречие."
                )
            ]
        )
    }
}
